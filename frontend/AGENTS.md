# Frontend Rules

- Make the design compact to all modules/components.
- Every enabled button must use a pointer cursor. With Tailwind CSS, include the `cursor-pointer` utility in the button's class list.
- For every deletion is make add always a modal for confirmation to delete data.